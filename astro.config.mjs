// @ts-check
import { defineConfig } from "astro/config";

// https://astro.build/config
export default defineConfig({
    // Your GitHub Pages site URL:
    site: "https://danko-miadria.github.io",
    // The repository path (only required if NOT using a custom domain or user page):
    base: "/astro-test-website",
});
